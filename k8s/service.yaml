apiVersion: v1
kind: Service
metadata:
  name: sap-datasphere-mcp
  namespace: datasphere-mcp-v2
spec:
  selector:
    app: sap-datasphere-mcp
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: abap-parser
      port: 8100
      targetPort: 8100
  type: ClusterIP
